import { defineConfig } from 'vitepress'

export default defineConfig({
    title: 'AI Ultra',
    themeConfig: {
        logo: '/logo.png',
        siteTitle: 'AI Ultra',
        search: {
        provider: 'local'
        }
  }
})

themeConfig: {
    i18nRouting: true
}